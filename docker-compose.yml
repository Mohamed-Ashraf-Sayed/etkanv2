services:
  web:
    build: .
    ports:
      - "3001:3000"
    env_file:
      - .env.local
    volumes:
      - uploads-data:/app/data/uploads
    restart: unless-stopped

volumes:
  uploads-data:
